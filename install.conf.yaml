- clean:
    - "~"
- link:
    ~/.gitconfig:
      force: true
    ~/.shell:
    ~/.zsh:
    ~/.zshrc:
      force: true
    ~/.oh-my-zsh/themes:
      force: true
      path: oh-my-zsh/themes
- shell:
    - [git submodule update --init --recursive, Updating the submodules]
