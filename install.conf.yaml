- clean:
    - "~"
- link:
    ~/.gitconfig:
      force: true
    ~/.shell:
    ~/.zsh:
    ~/.zshrc:
      force: true
- shell:
    - [git submodule update --init --recursive, Updating the submodules]
